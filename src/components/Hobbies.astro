---
const hobbies = [
	{
		title: "Guitar",
		description: "Passionate about playing guitar, exploring different genres and techniques.",
		icon: "🎸"
	},
	{
		title: "Custom Keyboards",
		description: "Building and customizing mechanical keyboards with different switches and layouts.",
		icon: "⌨️"
	},
	{
		title: "Music",
		description: "Love for J-rock, Russian hyper pop, and rap music.",
		icon: "🎵"
	}
];
---

<section class="hobbies" id="hobbies">
	<div class="container">
		<h2>Hobbies & Interests</h2>
		<div class="hobbies-grid">
			{hobbies.map((hobby) => (
				<div class="hobby-card">
					<div class="hobby-icon">{hobby.icon}</div>
					<h3>{hobby.title}</h3>
					<p>{hobby.description}</p>
				</div>
			))}
		</div>
	</div>
</section>

<style>
	.hobbies {
		padding: 6rem 2rem;
		background: rgb(var(--bg-tertiary));
		display: flex;
		align-items: center;
		transition: background-color 0.3s ease;
	}

	.container {
		max-width: 1000px;
		margin: 0 auto;
		width: 100%;
	}

	h2 {
		font-size: 2.5rem;
		color: rgb(var(--text-primary));
		margin-bottom: 3rem;
		text-align: center;
		transition: color 0.3s ease;
	}

	.hobbies-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.hobby-card {
		background: rgb(var(--bg-secondary));
		padding: 2rem;
		border-radius: 12px;
		box-shadow: 0 2px 4px rgba(var(--shadow-color));
		text-align: center;
		transition: all 0.3s ease;
		border: 1px solid rgba(var(--border-color));
	}

	.hobby-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 4px 8px rgba(var(--shadow-color));
	}

	.hobby-icon {
		font-size: 3rem;
		margin-bottom: 1rem;
	}

	h3 {
		color: rgb(var(--accent));
		font-size: 1.5rem;
		margin-bottom: 1rem;
	}

	p {
		color: rgb(var(--text-secondary));
		line-height: 1.6;
		transition: color 0.3s ease;
	}

	@media (max-width: 768px) {
		.hobbies {
			padding: 4rem 1rem;
		}

		.hobbies-grid {
			grid-template-columns: 1fr;
		}
	}
</style> 